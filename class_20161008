Option ExplicitConst SUMNAME = "Calculated Sum"'always write names of constants in uppercaseSub allWorksheets()Dim n As IntegerDim m As IntegerDim sName As WorksheetDim bName As Workbook    For m = 1 To Workbooks.Count        Set bName = Workbooks(m)            Debug.Print bName.Name                        For n = 1 To Worksheets.Count                Set sName = Worksheets(n)                                  Debug.Print bName.Worksheets(n).Name                        Next    Next m        'For n = 1 To Worksheets.Count     '   Set sheetName = Worksheets(n) 'when you assign values to variables, use set        '  Debug.Print sheetName.Name    'NextEnd SubSub AddWorksheet()Worksheets.Add after:=Worksheets(Worksheets.Count), Count:=4End SubSub worksheetSum()Dim n As IntegerDim wSum As SingleDim lineNumber As IntegerDim columnNumber As Integer    wSum = 0    lineNumber = 2        findWorksheet            'to delete all contents before start    Worksheets(SUMNAME).Range("outputarea").Value = ""    'the area/range can also be defined from the "Name Manager"        For n = 1 To 10        wSum = wSum + Worksheets(n).Cells(1, 1)                'fill out sum worksheet        Worksheets(SUMNAME).Cells(lineNumber, 1).Value = Worksheets(n).Name                'fill out sum worksheet        Worksheets(SUMNAME).Cells(lineNumber, 2).Value = Worksheets(n).Cells(1, 1).Value                lineNumber = lineNumber + 1    Next            With Worksheets(SUMNAME)        .Cells(lineNumber, 1).Value = "Sum:"        .Cells(lineNumber, 2).Value = wSum        .Range("A" & lineNumber & ":B" & lineNumber).Font.Bold = True        .Range("A" & lineNumber & ":B" & lineNumber).Font.Italic = False        .Range("A" & lineNumber & ":B" & lineNumber).Font.Underline = False    End WithEnd SubSub findWorksheet()Dim wSheet As WorksheetDim wSheetFound As Boolean    For Each wSheet In Worksheets        If wSheet.Name = SUMNAME Then            wSheetFound = True            Exit For            'in case wSheetFound is okay, we can exit the loop        End If    Next        If Not wSheetFound Then        Worksheets.Add(after:=Worksheets(Worksheets.Count)).Name = SUMNAME    End If    'add outputarea region        ActiveWorkbook.Names.Add Name:="outputarea", RefersToR1C1:= _        "='" & SUMNAME & "'!R1C1:R26C5"        End Sub
